# Streamlit Cloud secrets template.
#
# Copy to .streamlit/secrets.toml for local dev, or paste these keys into the
# "Advanced settings → Secrets" box when deploying to share.streamlit.io.
#
# .streamlit/secrets.toml is gitignored — never commit your real key.

ANTHROPIC_API_KEY = "sk-ant-api03-replace-with-your-key"

# Optional: gate the whole app behind a shared password.
# Leave blank or remove to disable.
SITE_PASSWORD = ""
